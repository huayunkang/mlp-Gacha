services:
  pony:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      CACHE_DIR: /app/cache
    volumes:
      - pony-cache:/app/cache
    restart: unless-stopped
volumes:
  pony-cache:
